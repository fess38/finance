CREATE TABLE Rubric(
	id INTEGER GENERATED BY DEFAULT AS SEQUENCE IdSequence NOT NULL PRIMARY KEY,
	name VARCHAR(100) NOT NULL UNIQUE,
	isIncome BOOLEAN NOT NULL,
	isService BOOLEAN DEFAULT FALSE NOT NULL,
	isDeleted BOOLEAN DEFAULT FALSE NOT NULL
);

INSERT INTO Rubric(id, name, isIncome, isService) VALUES
(10, 'Перевод между счетами', FALSE, TRUE);